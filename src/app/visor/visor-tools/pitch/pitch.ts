import { Component } from '@angular/core';
import { Tool } from '../warehouse/tool/tool';

@Component({
  selector: 'app-pitch',
  imports: [],
  templateUrl: './pitch.html',
  styleUrl: './pitch.scss',
})
export class Pitch extends Tool {

}
